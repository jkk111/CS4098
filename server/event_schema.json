[
  "CREATE TABLE IF NOT EXISTS event (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, location TEXT NOT NULL, description TEXT NOT NULL, start_time INTEGER NOT NULL, end_time INTEGER NOT NULL, max_attendees INTEGER NOT NULL, price INTEGER NOT NULL)"
]
[
  "CREATE TABLE IF NOT EXISTS user (id INTEGER PRIMARY KEY AUTOINCREMENT, f_name TEXT NOT NULL, l_name TEXT NOT NULL, username TEXT NOT NULL UNIQUE, password TEXT NOT NULL, email TEXT NOT NULL, phone TEXT NOT NULL DEFAULT \"\", email_verified BOOL NOT NULL DEFAULT 0, registered BOOL NOT NULL DEFAULT 1, is_admin BOOL NOT NULL DEFAULT 0, subscribed BOOL NOT NULL DEFAULT 0)"
]
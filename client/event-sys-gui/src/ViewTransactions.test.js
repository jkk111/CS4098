import React from 'react';
import ViewTransactions from './ViewTransactions.js';
import ReactDOM from 'react-dom';

describe('ViewTransactions', () => {
  it('should be defined', () => {
    expect(ViewTransactions).toBeDefined();
  });
});

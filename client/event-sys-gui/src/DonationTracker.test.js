import React from 'react';
import DonationTracker from './DonationTracker.js';
import ReactDOM from 'react-dom';

describe('DonationTracker', () => {
  it('should be defined', () => {
    expect(DonationTracker).toBeDefined();
  });
});
